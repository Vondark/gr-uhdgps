# auto-generated by grc.converter
'[Core]':
id: uhdgps_meta_to_json_file
label: PDU Meta to JSON File
category: uhdgps

parameters:
-   id: filename
    label: Filename
    dtype: string
    default: /tmp/pdus_%(hostname)s_%(time)s.json

inputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import uhdgps
    make: uhdgps.meta_to_json_file(${filename})

file_format: 1
