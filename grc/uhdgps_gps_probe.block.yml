# auto-generated by grc.converter
'[Core]':
id: uhdgps_gps_probe
label: GPS Probe
category: uhdgps

parameters:
-   id: target
    label: target
    dtype: string
    default: uhd_usrp_source_0

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import uhdgps
    make: uhdgps.gps_probe(self, ${target})

file_format: 1
