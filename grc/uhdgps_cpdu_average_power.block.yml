# auto-generated by grc.converter
'[Core]':
id: uhdgps_cpdu_average_power
label: CPDU Average Power
category: uhdgps

parameters:
-   id: k
    label: k (offset dB)
    dtype: int
    default: '-100'

inputs:
-   domain: message
    id: cpdus
    optional: true

outputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import uhdgps
    make: uhdgps.cpdu_average_power(${k})

file_format: 1
